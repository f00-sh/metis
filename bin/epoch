#!/usr/bin/env bash
# Metis 3.0 EPOCH flagship program
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export CL_SOURCE_REGISTRY="${ROOT}/:${CL_SOURCE_REGISTRY:-}"
exec sbcl --noinform --load "$ROOT/bin/epoch.lisp" -- "$@"
